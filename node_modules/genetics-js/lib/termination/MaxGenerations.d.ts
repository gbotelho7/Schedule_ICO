import { BaseIndividual } from '../individual/base/BaseIndividual';
import { Population } from '../population/Population';
import { TerminationCondition } from './TerminationCondition';
export declare class MaxGenerations<I extends BaseIndividual<T>, T> implements TerminationCondition<I, T> {
    private maxGenerations;
    constructor(maxGenerations: number);
    isSatisfied(population: Population<I, T>, generations: number): boolean;
}
