/**
 * ## Numeric Range
 * Represents a numeric range.
 */
export declare class NumericRange {
    static DEFAULT: NumericRange;
    static isArrayInRange(array: number[], range: NumericRange): boolean;
    static isValueInRange(value: number, range: NumericRange): boolean;
    static normalizeValueToRange(value: number, range: NumericRange): number;
    private static checkRangeValues;
    private static rangeValuesAreValid;
    private _lowest;
    private _highest;
    constructor(lowest?: number, highest?: number);
    get lowest(): number;
    set lowest(value: number);
    get highest(): number;
    set highest(value: number);
    set(lowest: number, highest: number): void;
}
