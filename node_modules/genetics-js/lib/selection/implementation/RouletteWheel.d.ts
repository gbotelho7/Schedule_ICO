import { BaseIndividual } from '../../individual/base/BaseIndividual';
import { Population, PopulationItem } from '../../population/Population';
import { SelectionImplementation, SelectionImplementationParams } from './SelectionImplementation';
export declare class RouletteWheel<I extends BaseIndividual<T>, T> implements SelectionImplementation<I, T> {
    select(population: Population<I, T>, params: SelectionImplementationParams, cumulativeProbabilityCallback: (item: PopulationItem<I, T>, index: number) => number): I[];
}
